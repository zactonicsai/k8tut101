---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-db-svc
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "demo.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
  - port: 5432
    targetPort: 3000
    protocol: TCP
